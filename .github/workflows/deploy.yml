name: Deploy to VPS

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /srv/containers/kohutka/app
            git pull

            # Regeneruj statický ceník při každém deployi
            node scripts/generate-pricing-json.js || echo "Pricing generation skipped"

            cd /srv/containers/kohutka
            docker compose build
            docker compose up -d
